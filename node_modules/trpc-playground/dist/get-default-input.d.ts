import { ProcedureSchemas } from '@trpc-playground/types';
import { Procedures, ProcedureTypes } from './zod-resolve-types';
export declare const getProcedureSchemas: (procedures: Procedures) => {
    schemas: ProcedureSchemas;
    types: ProcedureTypes;
};
